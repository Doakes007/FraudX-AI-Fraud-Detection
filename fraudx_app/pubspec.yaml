name: fraudx_app

description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^0.13.5
  provider: ^6.0.5

  # ✅ Added for device info and location access
  geolocator: ^10.1.0
  device_info_plus: ^9.1.0

  # ✅ NEW: SharedPreferences for storing backend IP
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.13.1

flutter_icons:   # ✅ App icon setup
  android: true
  ios: false
  image_path: "android/app/src/profile/photo_2025-07-10_13-26-43.jpg"

flutter:
  uses-material-design: true

